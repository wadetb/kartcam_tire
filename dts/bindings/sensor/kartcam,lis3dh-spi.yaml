description: |
  KartCam LIS3DH binding

compatible: "kartcam,lis3dh-spi"

include:
  - name: spi-device.yaml

properties:
  int-gpios:
    type: phandle-array
    description: Interrupt GPIO from the sensor
    required: false
